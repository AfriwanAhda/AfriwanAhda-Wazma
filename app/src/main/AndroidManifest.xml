<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="Wazma"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- AdMob App ID -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <activity
            android:name="motion.studio.wazma.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="motion.studio.wazma.activity.PlayActivity" />

        <!-- Premium/Paywall Activity -->
        <activity
            android:name="motion.studio.wazma.activity.PremiumActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Prayer Times Activity -->
        <activity android:name="motion.studio.wazma.activity.PrayerTimesActivity" />

        <!-- Settings Activity -->
        <activity android:name="motion.studio.wazma.activity.SettingsActivity" />

    </application>

</manifest>